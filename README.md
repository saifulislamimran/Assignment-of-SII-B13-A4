# 💼 Job Application Tracker - Assignment 04

স্বাগতম আমার **Job Application Tracker** প্রজেক্টে! এটি প্রোগ্রামিং হিরোর **Assignment-04** এর সাবমিশন।

---

## প্রজেক্টের অভিজ্ঞতা

এসাইনমেন্ট ৪ করতে গিয়ে জাভাস্কিপ্ট এর আসল মজা আর প্যারা দুইটাই বুঝলাম! ডম ম্যানিপুলেশন (DOM Manipulation) করতে গিয়ে মাঝে মধ্যে মাথা হ্যাং হয়ে যাচ্ছিল। বিশেষ করে যখন বাটন ক্লিক করে এক ট্যাব থেকে অন্য ট্যাবে ডাটা ফিল্টার করা আর ডিলিট করার লজিকটা মেলাতে যাচ্ছিলাম।

তবে হাল ছাড়িনি, বুঝে বুঝে লজিক বিল্ড করার চেস্টা করেছি। **Programming Hero** টিমকে অনেক অনেক ধন্যবাদ এত সুন্দর আর প্রাকটিক্যাল একটা এসাইনমেন্ট দেওয়ার জন্য। এটা করে নিজের উপর কনফিডেন্স অনেক বেড়ে গেছে!

---

## 🛠️ যা যা ব্যাবহার করেছি (Tech Stack)

- **HTML5** - **Tailwind CSS** (ডিজাইন এবং রেস্পন্সিভ করার জন্য)
- **Vanilla JavaScript** (লজিক এবং ডাইনামিক ফাংশনালিটির জন্য)

---

## 📝 Answers to the Questions

এখানে এসাইনমেন্টের ৫টা প্রশ্নের উত্তর আমার নিজের ভাষায় নিচে দেওয়া হলো:

**1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?**

- `getElementById`: এটা দিয়ে আমরা HTML এর id ধরে কোনো স্পেসিফিক ইলিমেন্ট খুজি। যেহেতু id ইউনিক হয়, তাই এটা শুধু একটা ইলিমেন্টই রিটার্ন করে।
- `getElementsByClassName`: এটা দিয়ে ক্লাস নাম ধরে খুজা হয়। একই ক্লাসের অনেক ইলিমেন্ট থাকতে পারে, তাই এটা একটা কালেকশন (HTMLCollection) রিটার্ন করে।
- `querySelector`: এটা দিয়ে আমরা CSS সিলেক্টর এর মত করে (যেমন `.class`, `#id`) ইলিমেন্ট খুজতে পারি। কিন্তু এটা শুধু প্রথম যে ইলিমেন্টটা পাবে সেটাই দিবে।
- `querySelectorAll`: এটাও CSS সিলেক্টর এর মত কাজ করে, কিন্তু ওই সিলেক্টরের আন্ডারে যতগুলা ইলিমেন্ট আছে সবগুলার একটা লিস্ট (NodeList) রিটার্ন করে।

**2. How do you create and insert a new element into the DOM?**
নতুন কোনো ইলিমেন্ট বানাতে হলে প্রথমে আমরা জাভাস্কিপ্ট এ `document.createElement('div')` (বা যে ট্যাগ চাই) লিখি। তারপর ওই ইলিমেন্টের ভিতরে ক্লাস বা টেক্সট বসাতে `innerHTML` বা `classList.add` ব্যাবহার করি। সবশেষে যে প্যারেন্ট ইলিমেন্টের ভিতরে এটাকে দেখাতে চাই, সেটাকে ধরে `appendChild()` বা `append()` করে ঢুকিয়ে দেই।

**3. What is Event Bubbling? And how does it work?**
ইভেন্ট বাবলিং হলো পানির বুদবুদ এর মত নিচ থেকে উপরে উঠা। মানে যখন আমরা কোনো ভেতরের ইলিমেন্টে (যেমন বাটনে) ক্লিক করি, তখন সেই ক্লিক ইভেন্টটা শুধু বাটনেই থেমে থাকে না। এটা আস্তে আস্তে তার উপরের প্যারেন্ট ইলিমেন্টগুলোর দিকে যেতে থাকে (যেমন button -> div -> section -> body)।

**4. What is Event Delegation in JavaScript? Why is it useful?**
ইভেন্ট ডেলিগেশন হলো অনেকগুলা চাইল্ড ইলিমেন্টে আলাদা আলাদা ইভেন্ট লিসেনার না লাগিয়ে, তাদের মেইন প্যারেন্ট এর মধ্যে একটা মাত্র লিসেনার লাগানো। বাবলিং এর কারনে চাইল্ডে ক্লিক করলে সেটা প্যারেন্ট এ জায়, তখন আমরা `event.target` দিয়ে চেক করে কাজ করতে পারি।
এটা খুব কাজের কারন এতে মেমরি অনেক বাচে আর জাভাস্কিপ্ট দিয়ে নতুন কোনো ইলিমেন্ট বানালেও সেগুলোতে আলাদা করে লিসেনার এড করতে হয় না।

**5. What is the difference between preventDefault() and stopPropagation() methods?**

- `preventDefault()`: এটা দিয়ে ব্রাউজারের ডিফল্ট কাজ বন্ধ করা হয়। যেমন ফর্মে সাবমিট বাটনে ক্লিক করলে পেজ রিলোড হওয়া আটকানো, বা কোনো লিংকে ক্লিক করলে অন্য পেজে যাওয়া আটকানো।
- `stopPropagation()`: এটা দিয়ে ইভেন্ট বাবলিং আটকানো হয়। মানে ভেতরের ইলিমেন্টে ক্লিক করলে সেই ইভেন্টটা যেন উপরের প্যারেন্ট পর্যন্ত না জায়, সেটা ওইখানেই থামিয়ে দেওয়া হয়।

---

### 💌 এক্সামিনারের প্রতি

আপনার জন্য অনেক শুভকামনা! কোডগুলো সিম্পল রাখার চেস্টা করেছি। ছোটখাটো কোনো ভুল বা টাইপিং মিসটেক থাকলে ক্ষমা সুন্দর দৃষ্টিতে দেখবেন।

**ধন্যবাদান্তে,**
Programming Hero ১৩তম ব্যাচের লার্নার **SII** ❤️
